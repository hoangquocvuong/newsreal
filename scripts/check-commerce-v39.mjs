import fs from 'node:fs';const h=fs.readFileSync('public/admin.html','utf8');const c=[['cache',h.includes('v=20260921-v39')],['one scroll',h.includes('body.admin-bg{overflow-y:auto!important')],['product manager',h.includes('async function refreshProducts()')],['edit',h.includes('data-edit=')],['delete',h.includes('data-del=')],['simple options',h.includes('commerce-option-v39')],['refresh after publish',h.includes('setTimeout(refreshProducts,1800)')]];let bad=0;for(const[x,o]of c){console.log((o?'PASS ':'FAIL ')+x);if(!o)bad++}if(bad)process.exit(1);console.log('Commerce Admin UX V39: PASS');