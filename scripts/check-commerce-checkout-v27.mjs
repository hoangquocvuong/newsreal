import fs from 'node:fs';let f=0;const ok=(v,m)=>{console.log((v?'OK ':'FAIL ')+m);if(!v)f++};const site=fs.readFileSync('public/assets/site.js','utf8'),api=fs.readFileSync('functions/api/[[path]].js','utf8');ok(site.includes('comQty')&&site.includes('com-qty'),'cart quantity controls');ok(['province','district','ward'].every(x=>site.includes(`name="${x}"`)),'full Vietnam address fields');ok(site.includes('shipping_method')&&site.includes('Giao hàng tiêu chuẩn'),'shipping method checkout');ok(site.includes('Mã giảm giá / voucher'),'voucher checkout');ok(api.includes("commerce/order-track"),'public order tracking API');ok(api.includes('shipping_method')&&api.includes('ward'),'order persists ward and shipping');ok(fs.existsSync('migrations/0059_commerce_checkout_production.sql'),'D1 migration 0059');if(f)process.exit(1);console.log('Commerce Checkout Production UX V27: PASS');
