import fs from'node:fs';const h=fs.readFileSync('public/admin.html','utf8');const tests=[['v40 cache',h.includes('v=20260921-v40')],['no product classification UI',!h.includes('Phân loại sản phẩm')],['no suggested attributes UI',!h.includes('Thuộc tính gợi ý')],['variants disabled',h.includes('const variants=[];')],['edit image preservation',h.includes('commerce-v40-edit-images')],['current gallery',h.includes('commerceCurrentImagesGrid')],['cover change',h.includes('data-cover')],['old image delete',h.includes('data-remove')],['edit status',h.includes('Chỉ chọn tệp nếu muốn thay ảnh.')] ];let bad=0;for(const[n,o]of tests){console.log((o?'PASS ':'FAIL ')+n);bad+=!o}if(bad)process.exit(1);console.log('Commerce Simple Seller V40: PASS');