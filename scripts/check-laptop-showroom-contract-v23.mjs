import fs from 'node:fs';
const fn=fs.readFileSync('functions/[[path]].js','utf8'),api=fs.readFileSync('functions/api/[[path]].js','utf8');let f=0;const ok=(x,m)=>{console.log((x?'OK ':'FAIL ')+m);if(!x)f++};
ok(fn.includes("c.kind==='laptop-store'")&&fn.includes('commerceDemoHome'),'dich-vu-5 dispatches to universal commerce renderer');ok(fn.includes('/demo/ban-hang/cua-hang-online'),'canonical commerce demo route exists');ok(!fn.includes('function evHome'),'obsolete EV renderer removed');ok(!fn.includes('leaflet.css')&&!fn.includes('leaflet.js'),'obsolete Leaflet dependency removed');ok(fn.includes('ASUS Vivobook 14'),'laptop retained as sample product');ok(fn.includes('Smartphone Pro 5G')&&fn.includes('Áo khoác Urban Essential'),'catalog is no longer laptop-only');ok(fn.includes('laptopLeadForm')&&fn.includes('/api/service-leads'),'customer lead inbox remains connected');ok(api.includes("card_contract:'commerce-product-card-v1'"),'commerce product card contract');if(f)process.exit(1);console.log('Commerce Migration Compatibility V23: PASS');
