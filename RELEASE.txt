NEWSREAL V20.9.5 — COC RENDERER SYMBOL RECOVERY

Critical fix
- Fixed the real Clash of Clans homepage renderer crash in V20.9.4.
- Root cause: renderGameClash1() called nrGameLevelCards(), but that renderer helper had been accidentally removed in the V20.9.1+ refactor.
- Restored nrGameLevelCards() from the last known-good Game renderer contract.
- Game demo posts now prefer the already-booted SITE_DATA payload and only fall back to the compiled sample array.
- Public Game demo remains isolated from the BDS tenant/API and cannot cross-fallback to a real-estate renderer.

Preserved
- Multi-criteria filter: Hall + Purpose/District + Year + Sort.
- Mobile staged filter drawer and explicit Apply button.
- Mobile hamburger navigation, no bottom navigation.
- Local-first Saved + visual toast.
- Unified Donate color and centered floating coffee button.
- Cloudflare/D1 batch stats contract.
- Dynamic template settings for Donate/About/Terms/Footer.

Contract
- game-1 structure profile version: 9
- boot_contract: game-renderer-symbol-complete-v1

No new D1 migration.
