NEWSREAL V20.9.3 — CoC Hard Boot + Multi-Criteria Filter Recovery

- Public /demo/game/clash-of-clans/ boots from its own local showroom package and never depends on the shared BDS tenant API.
- Absolute anti-cross-template fallback: an API failure can never expose the BDS transport shell.
- Asset/demo build cache-buster bumped to 20.9.3 and fast-nav session cache version bumped.
- Mobile filter is staged multi-criteria: choose Group + Level + Purpose + Year + Sort, then press Xem N bases.
- Desktop filter wraps cleanly and preserves all selected criteria.
- Donate buttons use one green color with no hover color/transform; circular coffee icon is centered.
- Keeps V20.9.1 stats/settings and V20.9.0 mobile-first contracts.
