NEWSREAL V20.9.6 — COC ROUTE + FILTER RUNTIME FIX

- Fixes Game About/Terms demo routes so they always serve the Game shell, never the BĐS static fallback.
- Restores missing nrGameFilterTypes() runtime helper that crashed /bases filter rendering in V20.9.5.
- Keeps multi-criteria filters: Hall + Level + Purpose/District + Year + Sort.
- Keeps mobile drawer, Saved toast, Donate consistency, D1 stats and dynamic template settings.
- Demo/static asset build bumped to 20.9.6.
- Game contract bumped to version 10 / game-route-runtime-complete-v2.
