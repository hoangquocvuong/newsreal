NEWSREAL V20.8.1 — COCBASE VPS → CLOUDFLARE PUBLISHER BRIDGE
- Adds authenticated Content Publisher Bridge V1 for the existing VPS auto-import pipeline.
- POST /api/publisher/base creates/updates Game Clash of Clans posts in D1.
- GET /api/publisher/check provides duplicate/idempotency lookup.
- GET /api/publisher/health verifies publisher capability.
- Stable external_key prevents duplicate posts during dual-publish migration.
- Stable /base/<slug>.html production routes now match the Game showroom article contract.
- Includes a VPS-side adapter reference without exposing publisher secrets to browser code.
- Migration: 0038_content_publisher_bridge.sql
