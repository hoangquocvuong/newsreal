# Commerce UX V42

- New product after edit now clears edit state and old image gallery from the form only.
- Product detail uses a single visible scrollbar: while the fixed detail panel is open, outer document scrolling is locked; closing detail restores page scrolling.
- V41 gallery persistence remains intact.
