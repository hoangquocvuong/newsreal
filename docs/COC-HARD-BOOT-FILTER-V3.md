# CoC Hard Boot & Filter V3

Public game showroom must never use the shared real-estate transport shell as a visible fallback. The demo route owns its renderer and local sample package.

Mobile filters are staged: chip selections update URL/state while the drawer remains open. Results are focused only after the user presses the Apply button. This supports combinations such as TH12 + Farming + 2026 + Vote.

Donate controls share one color and do not change color or transform on hover/focus.
